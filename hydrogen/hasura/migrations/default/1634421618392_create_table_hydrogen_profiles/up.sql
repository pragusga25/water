CREATE TABLE "hydrogen"."profiles" ("user_id" integer NOT NULL, "username" text NOT NULL, "email" text NOT NULL, "fullname" text NOT NULL, PRIMARY KEY ("user_id") , FOREIGN KEY ("user_id") REFERENCES "hydrogen"."users"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("user_id"), UNIQUE ("username"), UNIQUE ("email"));
